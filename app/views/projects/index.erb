<h1>Projects</h1>
<p>
    Public projects based on saved and submitted ideas.
</p>
<% Project.all.each do |project| %>
    <% if project.is_private == false %>
        <h3><%= project.name %></h3>
        <p><%= project.description %></p>
    <% end %>
<% end %>