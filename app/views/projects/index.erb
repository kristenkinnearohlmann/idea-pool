<h1>Projects</h1>
<% if flash[:msg] %>
    <span class="flash-msg"><%= flash[:msg] %></span>
<% end %>
<p>
    Public projects based on saved and submitted ideas. Visit a project to see details.<br/>
    Your projects marked as <em>private</em> will appear on your account dashboard.
</p>
<div class="category-display">
    <% Project.all.sort_by{|project| project.name}.each do |project| %>
        <% if project.is_private == false %>
            <div>
                <h4><a href="/projects/<%= project.id %>"><%= project.name %></a></h4>
                <p><%= project.description %></p>
            </div>
        <% end %>
    <% end %>
</div>