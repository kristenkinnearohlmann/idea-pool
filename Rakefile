# get environment configuration
require_relative './config/environment'
# add ActiveRecord tasks
require 'sinatra/activerecord/rake'

# add console task
desc 'A console'
task :console do
    Pry.start
end